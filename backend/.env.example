# Cổng chạy API
PORT=3000

# Prisma + MongoDB — LUÔN phải có TÊN DATABASE trong path (vd /timelinedb), nếu không sẽ lỗi
# "empty database name not allowed".
#
# 1) Docker local (single-node replica set): cần directConnection=true
# DATABASE_URL="mongodb://mongodb:27017/timelinedb?directConnection=true"
#
# 2) MongoDB local cài máy (single-node replica set):
# DATABASE_URL="mongodb://localhost:27017/timelinedb?directConnection=true"
#
# 3) MongoDB Atlas (cloud) — đã là replica set sẵn, KHÔNG cần directConnection.
#    Nhớ chèn /timelinedb trước dấu "?":
DATABASE_URL="mongodb+srv://<user>:<password>@<cluster>.mongodb.net/timelinedb?retryWrites=true&w=majority"

# JWT cho đăng nhập
JWT_SECRET="doi-thanh-mot-chuoi-ngau-nhien-dai"
JWT_EXPIRES_IN="7d"

# Zalo reminder bot
# Không commit token thật vào git. Điền endpoint/chat_id thật trong file .env local/server.
ZALO_REMINDER_ENABLED=false
ZALO_BOT_ENDPOINT="https://bot-api.zapps.me/<bot-id>:<bot-token>/sendMessage"
ZALO_CHAT_ID="<chat-id-nguoi-nhan>"
ZALO_REMINDER_LOOKAHEAD_HOURS=24
ZALO_REMINDER_INTERVAL_MINUTES=60

# Seed dữ liệu demo.
# Mặc định (false/bỏ trống): chỉ seed khi database trống -> dữ liệu bạn tự nhập
# (timeline, bài viết, từ vựng) KHÔNG bị mất khi khởi động lại backend.
# Đặt true: xoá sạch DB và dựng lại bộ demo mỗi lần khởi động — chỉ dùng khi phát triển.
SEED_RESET_ON_STARTUP=false

# Job tự đăng bài blog đã hẹn giờ (mặc định bật)
POST_SCHEDULE_ENABLED=true
POST_SCHEDULE_INTERVAL_MINUTES=30

# ---------- Module từ vựng ----------
# Dictionary (IPA, audio, definition, synonym/antonym) — miễn phí, không cần key.
VOCAB_DICTIONARY_ENDPOINT="https://api.dictionaryapi.dev/api/v2/entries/en"

# Gợi ý chính tả khi không tìm thấy từ (Datamuse) — miễn phí, không cần key.
VOCAB_SPELL_ENDPOINT="https://api.datamuse.com/words"

# Dịch dự phòng khi KHÔNG cấu hình ANTHROPIC_API_KEY (MyMemory).
VOCAB_TRANSLATION_ENABLED=true
# Điền email để MyMemory nới quota (tuỳ chọn)
VOCAB_TRANSLATION_EMAIL=""

# Claude — nghĩa tiếng Việt, ví dụ theo cấp độ, dạng khác của từ, CEFR.
# Bỏ trống thì hệ thống vẫn chạy, chỉ dùng dictionary + MyMemory.
ANTHROPIC_API_KEY=""
VOCAB_AI_MODEL="claude-opus-5"
